pipeline {
    agent { docker { image 'jenkins/agent' label 'my-docker-agent' }
    stages {
        stage('test 1 ') {
            steps {
                sh 'echo "asdddd"'
            }
        }
    }
}