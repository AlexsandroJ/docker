pipeline {
    agent any
    stages {
        stage('Back-end') {
            steps {
                sh 'git -v'
            }
        }
        stage('Back-end') {
            steps {
                sh 'docker -v'
            }
        }
    }
}